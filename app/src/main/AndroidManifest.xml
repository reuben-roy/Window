<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml — Window
    High-privilege monitoring application.
    PACKAGE_USAGE_STATS: requires user to grant manually via Settings > Apps > Special app access > Usage access.
    BIND_ACCESSIBILITY_SERVICE: requires user to enable in Settings > Accessibility.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- =====================================================================
         PERMISSIONS
    ====================================================================== -->

    <!-- Foreground service — required to keep the accessibility service alive -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Usage Stats — granted by user in system Settings (signature-protected) -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Post notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Internet — NOT requested intentionally; all AI inference is on-device -->

    <!-- =====================================================================
         APPLICATION
    ====================================================================== -->
    <application
        android:name=".WindowApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Window"
        tools:targetApi="31">

        <!-- -----------------------------------------------------------------
             MainActivity — Compose NavHost entry point
        ------------------------------------------------------------------ -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Window">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- -----------------------------------------------------------------
             WindowAccessibilityService
             BIND_ACCESSIBILITY_SERVICE is implicitly required when the
             service declares the accessibility intent-filter.
        ------------------------------------------------------------------ -->
        <service
            android:name=".service.WindowAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- -----------------------------------------------------------------
             PruneEventsWorker — background data retention via WorkManager
        ------------------------------------------------------------------ -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>

    </application>

</manifest>

